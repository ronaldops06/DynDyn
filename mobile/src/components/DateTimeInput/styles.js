import {StyleSheet} from "react-native";

export const getDateTimeInputStyle = (theme) => StyleSheet.create({
    areaDateTime: {
        flexDirection: "row",
        justifyContent: "space-between",
        alignItems: "center",
        width: "100%"
    }
});
